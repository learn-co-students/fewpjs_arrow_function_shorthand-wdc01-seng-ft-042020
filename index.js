function divide (){
    let num1 = 2000
    let num2 = 100
    return num1/num2
}

let square = (x) => x * x; 

let add = (x, y) => {
    return x + y
}

let add = (a,b) => a + b; 

let divide = () => 2000/100